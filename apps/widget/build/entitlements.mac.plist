<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Eine eigene Entitlements-Datei ERSETZT den Standardsatz von
       electron-builder — die JIT-Einträge müssen deshalb mit hier stehen.
       Ohne sie stirbt Electron beim Start mit „Failed to reserve virtual
       memory for CodeRange", weil V8 seinen Code-Bereich nicht anlegen darf. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Ad-hoc-Signatur und die vorgebauten Electron-Frameworks haben
       unterschiedliche Team-IDs. Ohne diese Ausnahme bricht die
       Library-Validierung den Start ab; die Hardened Runtime bleibt aktiv.
       Mit echtem Developer-ID-Zertifikat kann der Eintrag entfallen. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
